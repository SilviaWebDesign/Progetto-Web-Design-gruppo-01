# 3D components

Threlte/Three.js Svelte components that render 3D content.

All components must be used inside a Threlte `<Canvas>` element.
They cannot be used standalone (they use Threlte hooks like
`useTask` which require Canvas context).